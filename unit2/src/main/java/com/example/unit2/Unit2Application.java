package com.example.unit2;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
// Sets up the main application class for the Spring Boot application, which serves as the entry point for running the application.
@SpringBootApplication
public class Unit2Application {

	public static void main(String[] args) {
		SpringApplication.run(Unit2Application.class, args);
	}

}
